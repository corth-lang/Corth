include libs/linux_x86/io.corth
include libs/core.corth

// This example calculates the factorial of the number entered from the user

proc main
  int int -- int
in let argc argv in
  "Enter a number: " puts
  getu let number in
    "Factorial of " puts number putu " is " puts number fact putu "\n" puts
  end
end 0 end
